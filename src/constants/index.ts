export const TIER_MINIMUMS: Record<string, number> = {
  Gold: 8,
  Silver: 6,
  Bronze: 5,
}

export const HITL_THRESHOLD = 5000

export const RFQ_WORKFLOW_ID = import.meta.env.VITE_RFQ_WORKFLOW_ID as string
